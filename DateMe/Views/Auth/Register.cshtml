@model DateMe.ViewModels.RegisterViewModel
@{
    ViewBag.Title = "Register";
}

<h2>Register</h2>

@using (@Html.BeginForm())
{
    @Html.EditorForModel()

    <p>
        <button type="submit">Register</button>
    </p>
}

